{
  "name": "hv-pony-solver",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "overrides": {
      "protobufjs": "7.6.1",
      "vite": "8.0.16",
      "ws": "8.21.0"
    }
  },
  "scripts": {
    "build": "corepack pnpm -r build",
    "test": "corepack pnpm -r test && node --test scripts/model-manifest.test.mjs && node --test scripts/check-docs-drift.test.mjs && node --test scripts/check-graphify-corpus.test.mjs && node --test scripts/check-architecture-boundaries.test.mjs",
    "test:coverage": "corepack pnpm -r test:coverage",
    "typecheck": "corepack pnpm -r typecheck",
    "lint": "eslint .",
    "docs:check": "node scripts/check-docs-drift.mjs",
    "graphify:check": "node scripts/check-graphify-corpus.mjs",
    "architecture:check": "node scripts/check-architecture-boundaries.mjs",
    "format": "prettier --write .",
    "check:quick": "corepack pnpm lint && corepack pnpm typecheck && corepack pnpm test && corepack pnpm docs:check && corepack pnpm graphify:check && corepack pnpm architecture:check",
    "check": "corepack pnpm check:quick && corepack pnpm test:coverage && corepack pnpm build"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^22.0.0",
    "@vitest/coverage-istanbul": "4.1.6",
    "@vitest/coverage-v8": "4.1.6",
    "eslint": "^9.39.1",
    "prettier": "^3.7.4",
    "typescript": "^5.9.3",
    "vite": "8.0.16",
    "typescript-eslint": "^8.48.0",
    "vitest": "^4.1.0"
  }
}
